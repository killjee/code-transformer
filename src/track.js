
export class Track {
    constructor(key, argumentMap) {
        this.key = key
        this.argumentMap = argumentMap
    }
}

export class TrackArgument {
    constructor(key, value) {
        this.key = key
        this.value = value
    }
}